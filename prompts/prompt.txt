High / Critical (fix before wider use)
1) Defer heavy imports in CLI so simple commands and --help don't require full runtime
   - Files: src/economic_graph/cli.py
   - Change: move "from economic_graph.pipeline.runner import PipelineRunner" out of module top-level into the branch that needs it (inside main() just before creating the runner).
   - Example (replace the top-level import with lazy import inside main):
     - Remove top-level:
       from economic_graph.pipeline.runner import PipelineRunner
     - Add in main() where runner is used:
       if args.run or not (args.init or args.report):
           from economic_graph.pipeline.runner import PipelineRunner
           runner = PipelineRunner(config)
   - Priority: Critical
   - Why: avoids import-time failures if heavy deps (torch, numpy) are missing; makes help and packaging operations safe.
   - Verify: run "python -m economic_graph.cli --help" in an environment without heavy deps — it should show help without ImportError.

2) Fix packaging configuration so wheel includes the src package (src layout -> package name)
   - Files: pyproject.toml
   - Change: replace the current wheel package entry that uses a path with a package name / proper src-layout config. At minimum, change packages = ["src/economic_graph"] to packages = ["economic_graph"] and ensure the build knows that packages live under src.
   - Suggested follow-up: add or confirm a package-dir mapping / use hatchling's recommended config for src-layout (or use setuptools with [tool.setuptools.packages.find] and package-dir = { "": "src" }).
   - Example verification commands:
     - Build wheel: hatch build or python -m build
     - Inspect wheel contents: unzip -l dist/*.whl | grep economic_graph
   - Priority: Critical
   - Why: current config likely omits package or fails to build/install correctly.

3) Make CLI require explicit --run (avoid running pipeline by default)
   - Files: src/economic_graph/cli.py
   - Change: change condition:
     - Current: if args.run or not (args.init or args.report):
     - Replace with: if args.run:
   - Priority: High
   - Why: running the pipeline by default is surprising and can launch long/expensive jobs inadvertently.
   - Verify: run "python -m economic_graph.cli" — it should not start pipeline; "python -m economic_graph.cli --run" should.

High / Important repo & docs fixes
4) Fix README clone/usage instructions and canonical names
   - Files: README.md
   - Change:
     - Update clone URL and cd command to match this repo:
       git clone https://github.com/DanielBerns/economic_paper.git
       cd economic_paper
     - Align project names across README, pyproject, and packaging (show using package import "economic_graph").
     - Choose and document whether project uses uv or hatch/hatchling (see next item).
   - Priority: High
   - Why: current instructions reference a different repo/name and conflicting tooling.

5) Choose and document project tooling (uv vs hatchling) consistently
   - Files: README.md, pyproject.toml
   - Change:
     - To use "uv": update pyproject to match uv (or add uv config) and document uv sync/run/test sequences.
   - Priority: High
   - Why: conflicting tooling in README vs pyproject confuses contributors and CI.

6) Add CI workflow running tests on push/PR
   - Files to add: .github/workflows/python-tests.yml
   - Example content (minimal):
     - name: CI
       on: [push, pull_request]
       jobs:
         test:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - name: Set up Python
               uses: actions/setup-python@v4
               with:
                 python-version: "3.14"
             - name: Install deps
               run: |
                 python -m pip install --upgrade pip
                 pip install -e .
                 pip install pytest
             - name: Run tests
               run: python -m pytest -q
   - Priority: High
   - Verify: push branch and confirm CI runs, or run the commands locally.

Medium / Fixes that avoid runtime errors or confusion
7) Ensure log file parent directory exists before adding file handler
   - Files: src/economic_graph/logging.py (you earlier noted file exists)
   - Change: before creating a FileHandler / RotatingFileHandler, create parent directory:
     - import os
     - log_dir = os.path.dirname(log_file)
       if log_dir:
           os.makedirs(log_dir, exist_ok=True)
   - Priority: Medium
   - Why: prevents errors when writing logs to a non-existent logs/ directory.
   - Verify: run the app and confirm logs/economic_graph.log is created.

8) Make --create behavior explicit and exit after creating config (UX)
   - Files: src/economic_graph/cli.py and/or src/economic_graph/config.py
   - Change:
     - Option A: In cli.main, if args.create is set: call read_config(args_list=args_list) or directly create file and exit with success message.
     - Option B: In read_config, when creating file because should_create True, print message and return early; but ensure CLI exits or informs user that creation succeeded instead of continuing to run pipeline automatically.
   - Priority: Medium
   - Verify: run CLI with --create and ensure a config/settings.yaml is created and CLI exits.

9) Make ruff target-version a list (format consistency)
   - Files: pyproject.toml
   - Change: change target-version = "py312" to target-version = ["py314"]
   - Priority: Medium
   - Verify: run ruff lint locally and confirm it respects configuration.

10) Align project naming (pyproject.name, repository name, package name)
    - Files: pyproject.toml, README.md, repository description
    - Change: choose one canonical name: e.g., project name = "economic_paper" or "economic-graph", and update all references. Ensure package name (economic_graph) corresponds to intended PyPI/project name if publishing.
    - Priority: Medium
    - Why: prevents confusion for users and publishing.

Low / housekeeping and repo hygiene
11) Remove or justify committed build artifacts and lock files
    - Files: uv.lock, latex/main.pdf (and other PDF or binary artifacts)
    - Change options:
      - If uv.lock is required for deterministic dependency management, document why in README.
      - Otherwise, add uv.lock and build artifacts (latex/*.pdf, latex/*.aux) to .gitignore and remove them from history.
      - Example .gitignore entries:
        uv.lock
        latex/*.pdf
        latex/*.aux
    - Priority: Low
    - Verify: git status should not show these files after removal and update.

12) Add LICENSE, CONTRIBUTING, and basic CODE_OF_CONDUCT
    - Files to add: LICENSE (e.g., MIT), CONTRIBUTING.md, CODE_OF_CONDUCT.md
    - Change: add an appropriate license and contributor documentation.
    - Priority: Low
    - Verify: presence of these files in repo root; choose and apply license in repo settings.

13) Add a short README section about configuration and example config content
    - Files: README.md
    - Change: add a copy-paste example of config/settings.yaml or reference config/settings.example.yaml and show the uv/hatch commands used to generate it.
    - Priority: Low
    - Verify: README clearly describes how to create and edit configuration.

14) Tests: ensure tests are runnable in isolation and CI installs dev dependencies
    - Files: pyproject.toml, .github/workflows/python-tests.yml
    - Change: add dev optional-dependencies to pyproject and ensure CI installs them before running pytest (pip install -e .[dev]).
    - Priority: Low
    - Verify: pytest passes locally after installing dev dependencies.

Concrete verification checklist (commands)
- After changes, locally test:
  - python -m economic_graph.cli --help
  - python -m economic_graph.cli --create
  - python -m economic_graph.cli --run (in an environment with dependencies)
  - hatch build (or python -m build) and inspect wheel: unzip -l dist/*.whl | grep economic_graph
  - python -m pytest
- For CI: push a branch and verify the workflow triggers and passes.


