Write a research paper using info from the uploaded file, the paper in the url https://openreview.net/pdf?id=Sv7DazuCn8 and the following text:
Agents have an internal state, a goal state (desired state) and a controller that receives inputs and delivers the next outputs and state update.
Assets, or inert things, have a state but not goal state, and a controller that receives inputs and delivers the next outputs and state update. 
The state of the agents have two parts: a digital state (updated via computation), and an inventory (a set of assets) updated via interactions between the agent and the asset.
Agents and assets are subject to time induced decay.
Some agents may be able to learn online continuously from the inputs and 
Environment has a state and a controller that receives inputs and delivers the next outputs and state update. 
Agents and assets are parts of the environment.
Agents, assets and the environment interact between them.
The interactions are edges and agents, assets and the environment are nodes of a graph
